apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - litellm.yaml
  - openclawinstance.yaml
  - ingress.yaml
  - networkpolicy-mattermost.yaml
  - secrets/
# REMOVED:
#   helmrelease.yaml         — replaced by openclawinstance.yaml (managed by openclaw-operator)
#   envoy-proxy-config.yaml  — required Envoy Gateway CRDs (not installed); caused crash loop
#   hpa-chromium.yaml        — Chromium is disabled (resource risk on t3.medium)
